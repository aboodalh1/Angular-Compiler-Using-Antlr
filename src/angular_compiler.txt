import {Component} from "angular";

@Component({
    selector: 'app-example',
    template: '<div>Inline template</div>',
    templateUrl: 'example.component.html',
})
export class ExampleComponent {
    private http: HttpClient; // Not imported - should trigger an error
    x:number = 10;
    constructor() {
        this.http = new HttpClient(); // Not imported - should trigger an error
    }
}