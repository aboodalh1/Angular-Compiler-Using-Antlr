// Default Angular Compiler Test File - FIXED SYNTAX
import { Component } from '@angular/core';

@Component()
export class AppComponent {
    title: string = "Angular Compiler Test App";
    subtitle: string = "Welcome to the Test Application";
    description: string = "This is a test application to demonstrate the Angular compiler capabilities.";
    isLoading: boolean = false;
    buttonText: string = "Click Me!";
    
    features: Array = ["Component-based architecture", "Template-driven forms", "Reactive programming", "Dependency injection", "Routing and navigation"];
    
    onButtonClick() {
        console.log("Button clicked!");
        this.buttonText = "Clicked!";
        // Note: setTimeout with arrow functions not supported in this grammar
        // Using simple assignment instead
        this.buttonText = "Click Me!";
    }
}
